<section>
  <h1><%= headline %></h1>
  <p><%= description %></p>
  <p>This website uses Express and EJS to render pages dynamically on the server.</p>
  <p>Change the page content in the `views/` folder and update the `team` data in `server.js`.</p>
</section>
